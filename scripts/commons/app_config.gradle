/**
 * App-only configuration
 * */
apply plugin: "com.android.application"
android.defaultConfig.applicationId = "com.cioccarellia.buildsample"

apply from: rootProject.file("scripts/commons/common_config.gradle")