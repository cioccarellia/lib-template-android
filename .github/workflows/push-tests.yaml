name: Run Tests

on:
    push:
    pull_request:

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Project Checkout
              uses: actions/checkout@v2

            - name: Setup JDK 11
              uses: actions/setup-java@v1
              with:
                  java-version: '11'

            - name: Build project
              run: ./gradlew build --stacktrace

            - name: "Run tests"
              run: ./gradlew test # testDebug or testRelease (if you have)
